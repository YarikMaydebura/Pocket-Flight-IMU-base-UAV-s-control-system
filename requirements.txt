# ROS Python library (install via apt on Ubuntu)
# sudo apt-get install ros-noetic-rospy

# Python dependencies
numpy>=1.19.0
websocket-client>=1.0.0

# Optional (for data_visualizer.py)
matplotlib>=3.3.0
